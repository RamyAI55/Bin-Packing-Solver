To run the program, you must have all the requirements in requirements.txt met.

You should run main.py using python or any IDE, then everything is usable via the GUI.

For repeated runs, restart the program.